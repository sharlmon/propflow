name: Ecosystem quality gates

on:
  pull_request:
  push:
    branches: [develop, main]

permissions:
  contents: read

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm, cache-dependency-path: package-lock.json }
      - run: npm ci
      - run: npm run format:check
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run test
      - run: npm run build

  backend:
    runs-on: ubuntu-latest
    defaults: { run: { working-directory: services/api } }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with: { go-version: '1.25.x', cache-dependency-path: services/api/go.sum }
      - run: go mod download
      - name: Verify formatting
        run: test -z "$(gofmt -l .)"
      - run: go vet ./...
      - run: go test ./...
      - run: go test -race ./...

  database-and-contract:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env: { POSTGRES_USER: propflow, POSTGRES_PASSWORD: propflow_test_only, POSTGRES_DB: propflow_ci_test }
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U propflow -d propflow_ci_test"
          --health-interval 5s --health-timeout 3s --health-retries 20
    env:
      TEST_DATABASE_URL: postgres://propflow:propflow_test_only@localhost:5432/propflow_ci_test?sslmode=disable
      PGPASSWORD: propflow_test_only
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm, cache-dependency-path: package-lock.json }
      - uses: actions/setup-go@v5
        with: { go-version: '1.25.x', cache-dependency-path: services/api/go.sum }
      - run: psql -h localhost -U propflow -d propflow_ci_test -v ON_ERROR_STOP=1 -f services/api/migrations/000001_initial.up.sql
      - run: psql -h localhost -U propflow -d propflow_ci_test -v ON_ERROR_STOP=1 -f services/api/migrations/000002_seed.up.sql
      - run: psql -h localhost -U propflow -d propflow_ci_test -v ON_ERROR_STOP=1 -f services/api/migrations/000003_ecosystem_foundation.up.sql
      - run: psql -h localhost -U propflow -d propflow_ci_test -v ON_ERROR_STOP=1 -f services/api/migrations/test/000003_foundation_assertions.sql
      - run: test "$(psql -h localhost -U propflow -d propflow_ci_test -Atc "select count(*) from listings where status='published'")" -ge 6
      - run: npm ci
      - run: npm run contract:check
        working-directory: apps/platform-web
      - run: go run github.com/sqlc-dev/sqlc/cmd/sqlc@v1.30.0 generate -f sqlc.yaml
        working-directory: services/api
      - run: git diff --exit-code -- services/api/sqlc

  golden-path:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env:
          { POSTGRES_USER: propflow, POSTGRES_PASSWORD: propflow_test_only, POSTGRES_DB: propflow_e2e_test }
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U propflow -d propflow_e2e_test"
          --health-interval 5s --health-timeout 3s --health-retries 20
    env:
      TEST_DATABASE_URL: postgres://propflow:propflow_test_only@localhost:5432/propflow_e2e_test?sslmode=disable
      PGPASSWORD: propflow_test_only
      APP_ORIGINS: http://127.0.0.1:3000,http://127.0.0.1:3001
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm, cache-dependency-path: package-lock.json }
      - uses: actions/setup-go@v5
        with: { go-version: '1.25.x', cache-dependency-path: services/api/go.sum }
      - run: psql -h localhost -U propflow -d propflow_e2e_test -v ON_ERROR_STOP=1 -f services/api/migrations/000001_initial.up.sql
      - run: psql -h localhost -U propflow -d propflow_e2e_test -v ON_ERROR_STOP=1 -f services/api/migrations/000002_seed.up.sql
      - run: psql -h localhost -U propflow -d propflow_e2e_test -v ON_ERROR_STOP=1 -f services/api/migrations/000003_ecosystem_foundation.up.sql
      - run: npm ci
      - run: npx playwright install --with-deps chromium
        working-directory: apps/platform-web
      - run: npm run test:e2e
        working-directory: apps/platform-web
      - uses: actions/upload-artifact@v4
        if: failure()
        with: { name: playwright-report, path: apps/platform-web/playwright-report, retention-days: 7 }

  security-and-images:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm, cache-dependency-path: package-lock.json }
      - uses: actions/setup-go@v5
        with: { go-version: '1.25.x', cache-dependency-path: services/api/go.sum }
      - run: npm ci
      - run: npm audit --audit-level=high
      - name: Scan repository history for secrets
        uses: gitleaks/gitleaks-action@v2
      - run: go install golang.org/x/vuln/cmd/govulncheck@latest
      - run: govulncheck ./...
        working-directory: services/api
      - run: docker build -f services/api/Dockerfile -t propflow-api:ci .
      - run: docker build -f apps/platform-web/Dockerfile -t propflow-platform-web:ci .
      - run: docker build -f apps/jengabora-web/Dockerfile -t propflow-jengabora-web:ci .
      - name: Start the complete Compose demo topology
        run: docker compose up -d --build --wait --wait-timeout 120
      - name: Smoke web and API health
        run: |
          curl --fail --retry 5 --retry-delay 2 http://localhost:8080/healthz
          curl --fail --retry 5 --retry-delay 2 http://localhost:8080/readyz
          curl --fail --retry 5 --retry-delay 2 http://localhost:3000/
          curl --fail --retry 5 --retry-delay 2 http://localhost:3001/
          curl --fail --retry 5 --retry-delay 2 http://localhost:9000/minio/health/live
      - name: Show Compose logs after failure
        if: failure()
        run: docker compose logs --no-color
      - name: Stop Compose demo topology
        if: always()
        run: docker compose down -v
